# Simple GPT-2 on WikiText-103

This repository trains a minimal GPT-2 style model autoregressively on the WikiText-103 dataset using PyTorch and SentencePiece.

## Setup

1. Install requirements:
   ```bash
   pip install -r requirements.txt